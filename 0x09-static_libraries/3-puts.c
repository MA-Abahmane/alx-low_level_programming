#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * _puts - a function that prints a string, followed by a new line.
 * @str: given string
 * Return: None
 */

void _puts(char *str)
{
puts(str);
}
