#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * _strlen - function that returns the length of a string.
 * @s: given string
 * Return: None
 */

int _strlen(char *s)
{
return (strlen(s));
}
