#include <stdio.h>
#include <ctype.h>
#include "main.h"

/**
* _isdigit - find out if the int c is a number
* @c: given int
* Return: 0 alltimes
*/

int mul(int a, int b)
{
int multi;

multi = a * b;
printf("%d", multi);
 
return (multi);
}
