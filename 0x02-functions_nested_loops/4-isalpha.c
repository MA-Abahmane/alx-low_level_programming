#include "main.h"

/**
 * main - Entry point
 *
 * Description: [Print [a..z\n] from another function]
 *
 * Return: Always 0
 *
 * code by: MAAbahmane
 */

int _isalpha(int c)
{
if (isalpha(c))
{
return (1);
}
else
{
return (0);
}
}
