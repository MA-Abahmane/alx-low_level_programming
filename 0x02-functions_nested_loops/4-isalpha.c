#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point
 *
 * Description: [Print [a..z\n] from another function]
 *
 * Return: Always 0
 *
 * code by: MAAbahmane
 */

int _isalpha(int c)
{
if (_isalpha(c))
{
return (1);
}
else
{
return (0);
}
}
