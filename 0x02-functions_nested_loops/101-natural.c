#include "main.h"
#include <stdio.h>

/**
 * print_times_table - print tame table of given num
 * @num:  given num
 * Return:  the table of numbers
 */

void naturals(void)
{
int i = 0;

while (i < 1024)
{
if (i % 5 == 0 || i % 3 == 0)
{
i = i;
}
}
printf("%d", i);

return;
}
