#include "main.h"
#include <ctype.h>

/**
 * the _islower - find out if letter is upper or lower cased
 */
int _islower(int c)
{
if (islower(c))
{
/**
 * u_int - Typedef for unsigned int
 */
return (1);
}
else
{
/**
 * u_int - Typedef for unsigned int
 */
return (0);
}
}
