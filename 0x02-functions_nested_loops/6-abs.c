#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * _abs - find oabselute value
 * @n:  given value
 * Return:  abselute value of given number
 */
int _abs(int n)
{
int abs_val = abs(n);
return (abs_val);
}
