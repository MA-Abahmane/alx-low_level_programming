#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * main.h - Entry point
 *
 * Description: [A a function that computes the absolute value of an integer]
 *
 * Return: Always abs_val
 *
 * code by: MAAbahmane
 */

int _abs(int n)
{
int abs_val = abs(n);
return (abs_val);
}
