#include <stdio.h>
#include <ctype.h>
#include "main.h"

/**
* _isupper - find out if the char c is uppercase
* @c: given car
* Return: 0 alltimes
*/

int _isupper(int c)
{
if (isupper(c))
{
return (1);
}
else
{
return (0);
}
}
