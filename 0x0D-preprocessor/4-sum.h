#include <stdio.h>
#include "4-sum.h"
#include "4-sum.h"
#define SUM(a, b) ((a) + (b))

/**
 * main - a function-like macro SUM(x, y)
 * that computes the sum of the numbers x and y.
 *
 * Return: Always 0.
 */
int main(void)
{
int s;

s = SUM(98, 1024);
printf("%d\n", s);
return (0);
}
