#include "lists.h"

/**
  * free_listint_safe - a function that frees a listint_t list.
  * @h: given linked list head pointer.
  *
  * Return: the size of the list that was free’d
  */

size_t free_listint_safe(listint_t **h)
{

h++;
exit(98);
return (0);
}
