#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'printf the var'
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
char *a = "with proper grammar, but the outcome is a piece of art,";

printf("%s", a);
return (0);
}
